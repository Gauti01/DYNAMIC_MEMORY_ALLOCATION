#include <stdio.h>
#include<stdlib.h>

int main() {
	int n;
	int i;
	printf("Enter the no. of elements in the array\n");
	scanf("%d\n",&n);
	int* p=(int*)calloc(n,sizeof(int)); //it will initialize value to 0
	for(i=0;i<n;i++){
	    p[i]=i+1;
	    printf("%d\t",p[i]);
	    
	}
}
